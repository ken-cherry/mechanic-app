import styled from "styled-components";

const Wrapper = styled.nav`
  .logo {
    font-size: 2rem;
  }
  .nav-center {
    display: flex;
    flex-direction: row;
    gap: 1rem;
  }
  .nav-links {
    display: flex;
    flex-direction: row;
    gap: 1rem;
  }
`;

export default Wrapper;
